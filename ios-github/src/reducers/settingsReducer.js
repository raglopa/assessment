const defaultState = {
  repo: {
    owner: 'Alamofire',
    repo: 'Alamofire'
  }
}

export default (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
